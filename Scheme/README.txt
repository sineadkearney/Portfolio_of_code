College assignment, assignment requirements:

Add a "macro" facility to the meta-circular Scheme evaluator from
lecture.  Use it to implement "let" and "cond" by expanding such
forms into a simple lambda, and a series of nested if's, respectively.